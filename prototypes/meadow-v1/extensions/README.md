# Prototype extensions

Optional TypeScript hooks for behavior unique to one prototype.
Prefer config flags for anything the template already supports.

Shared type: `prototypes/_shared/extensions.ts`.
