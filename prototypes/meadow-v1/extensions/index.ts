import type { PrototypeExtensions } from "../../_shared/extensions.js";

/**
 * Optional prototype-unique hooks. Keep empty unless this game needs
 * special behavior beyond the tile-board template.
 */
export const extensions: PrototypeExtensions = {
  // Example: banner: "Meadow playtest",
};

export default extensions;
