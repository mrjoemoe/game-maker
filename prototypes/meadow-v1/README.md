# Meadow v1

Prototype of the `tile-board` template.

```bash
./dev.sh up meadow-v1
# or another port while another prototype is running:
./dev.sh up meadow-v1 5174
```

Edit `config/game.config.ts` for tiles, names, flip, pieces. Put unique code in `extensions/`.
