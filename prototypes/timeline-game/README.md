# Timeline Game

Launch:

```bash
./dev.sh up timeline-game
```

Optional port: `./dev.sh up timeline-game 5175`

Composed from `core/timeline`. Debug mode is on by default so you can move, draw, play, and use every device without resource checks.
