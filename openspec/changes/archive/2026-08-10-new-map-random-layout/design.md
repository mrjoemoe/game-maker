## Decisions

1. Extend RandomTilePlacement with `count` (default 1); each pick removes that cell from further meadow candidates.
2. Goblin Woods content counts match the old fixed layout totals.
3. Exclude start + corners from all random placements.

## Open Questions

None.
