## Decisions

- Keep collect-on-safe-land; do not show coin counts on face-down tiles (fog of war).
- Move tile coin badge to bottom-right so the hero piece (top-right) does not cover it.
- On pickup, set run bump to “Collected N coin(s) — wallet X”.
- HUD shows a dedicated wallet chip with icon + large count.
