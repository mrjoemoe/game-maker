## Decisions

`programLength` is a maximum, not a fixed count. Minimum program size is 1 step. After a short program finishes while still playing, the player may chart another path from the current position.
