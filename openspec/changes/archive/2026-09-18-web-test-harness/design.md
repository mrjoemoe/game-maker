## Decisions

- Node environment is enough; no React renderer. Test pure store helpers and existing assert-style timeline files via Vitest.
- Alias `@game-maker/engine` in the web Vitest config the same way Vite does.
