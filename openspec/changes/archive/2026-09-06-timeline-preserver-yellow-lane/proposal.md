## Why

A preserved branch is easy to miss: only the mat gets a thin lock outline. The locked timeline itself should read as locked at a glance.

## What Changes

- Paint a yellow wash behind a preserved branch’s full column (cards and mat).

## Impact

- **modified:** `playtest-web-app` (timestream chrome)
- **reused:** Preserver engine flag
