## Decisions

- CSS-only density for the header; no new routing.
- Under-board row is a wrapper in `App.tsx` so inventory stays left of tally/debug.
- Action track stays 10 columns; small viewports scroll horizontally rather than wrapping to 5.
