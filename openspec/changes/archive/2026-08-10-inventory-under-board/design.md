## Decisions

- Board column = board + inventory strip; sidebar = planner + tile tally.
- Inventory section shows coins, on-person, and stash (loadout stays with stash).
- Remove the dedicated coin chip from the top RunHud to avoid duplicate wallets.
