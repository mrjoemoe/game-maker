## Context

See proposal. Builds on existing archive-and-push + OpenSpec skills.

## Decisions

1. **Mandatory OpenSpec** — even “can you also…” tweaks need a change folder before code.
2. **Fast-track** — slim artifacts + same archive/commit/push ending; not a bypass.
3. **Docs in three places** — `AGENT.md` (authoritative), skill (how-to), `openspec/config.yaml` (CLI/agent context).

## Open Questions

None.
