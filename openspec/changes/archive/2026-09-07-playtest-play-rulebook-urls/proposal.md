## Why

Play and Rulebook are only in-page tabs, so you cannot bookmark or share the rulebook, and refresh always lands on Play.

## What Changes

- Play lives at `/play`. Rulebook lives at `/rulebook`. `/` shows Play.
- Switching tabs updates the path without resetting the playtest session.

## Impact

- **modified:** `playtest-web-app` (tab routing)
