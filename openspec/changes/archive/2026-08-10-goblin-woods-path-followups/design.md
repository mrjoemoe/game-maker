## Decisions

Implemented already in code; this change documents and archives the shipped behavior. Safe path = empty-effect tiles only; hazards/walls/goal end the run with `markLost` + message; program execution stops when status ≠ playing.

## Open Questions

None — retroactive archive of playtest iterations.
