## Why

The right-side action bank wastes space on instructional copy, a separate move pad, and the tile tally, so fewer actions are visible at once.

## What Changes

- Remove the action-bank lede text
- Place move actions in the same choice list as other actions
- Move the tile tally out of the right sidebar (below inventory) so the bank can use the height

## Impact

- Capability: `playtest-web-app`
- Components: none (UI layout)
